# Generated by `seed` -- contains plaintext tenant bearer tokens (returned
# exactly once by /admin/register-tenant) and cached reference data. Never
# commit this; it's local-run output, not source.
.tpcc-tenants*.json
