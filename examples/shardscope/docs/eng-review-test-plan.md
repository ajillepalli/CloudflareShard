# Test Plan
Generated by /plan-eng-review on 2026-07-16
Branch: feature/tpcc-benchmark
Repo: ajillepalli/CloudflareShard

## Affected Pages/Routes
- /admin/vbucket-map (new) — catalog-aware fan-out merge; mid-migration row must show migration_status + target_shard_id
- Shardscope SSE stream — one upstream poll/tick regardless of subscriber count
- Shardscope Topology/Reshard/App/Playground rooms

## Key Interactions to Verify
- Trigger split/migrate/drain under load; topology animates; scoreboard holds lost:0
- Playground blocks browser-side /v1/sql + /v1/scatter (admin-gated) or requires Shardscope operator gate
- App room 2PC action is same-tenant, <=8 participants, labeled accurately
- Routing inspector resolves catalog THEN vbucket ({catalogShardId, vbucket})

## Edge Cases
- Isolate eviction mid-demo: tenant token survives (durable store), no rotation of harness-cached tokens
- Worker-native load engine hits CPU/subrequest cap: visible degradation, not silent stall
- Skew driver overshoots: shard too hot to drain under load
- Correctness reader on non-moving keys: MUST NOT show false green

## Critical Paths
- CRITICAL: inject a lost/stale write during cutover -> correctness meter goes RED (guards against false-green demo)
- Reshard-under-load with dashboard polling stays within OLTP throughput-delta budget
