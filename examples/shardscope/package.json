{
  "name": "cloudflare-shard-shardscope",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "Shardscope: a live mission-control dashboard for CloudflareShard, service-bound to the main Worker's RPC entrypoint (see examples/rpc-consumer). Phase 1 skeleton — see README.md.",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20260610.0",
    "typescript": "^5.9.2",
    "wrangler": "^4.24.0"
  }
}
