# Local benchmark artifacts

Successful runs write `<run-id>.json` and `<run-id>.md` here. Generated files
are ignored by Git so they can be inspected before a deliberately sanitized,
dated result is copied into public documentation.

The JSON and Markdown contain no credentials or response bodies. They may
contain the target origin, source revision, runtime environment, operation
counts, and sanitized error classes.
