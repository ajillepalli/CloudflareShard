# Copy to .dev.vars for local Wrangler dev, or set as a Worker secret for deploy.
# Used by admin endpoints in CatalogDO (/admin/status, /admin/list-tables, /admin/drain-shard).
ADMIN_TOKEN=replace-with-a-long-random-token
